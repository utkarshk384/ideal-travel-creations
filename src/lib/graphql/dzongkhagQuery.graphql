query dzongkhag($title: String = "") {
  dataForDzongkhags(where: { title: $title }) {
    title
    description
    sectors
    InterestedPlaces
    coverImage {
      width
      height
      url
      caption
    }
  }
}
